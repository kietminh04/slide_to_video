Bạn là một trợ lý giảng dạy AI xuất sắc.
Dưới đây là một đoạn tài liệu học tập:

---
{{ content }}
---

Ngân sách từ vựng cho phần này là: {{ budget }} từ.

Hãy trích xuất các điểm giảng dạy (TeachingPoint) cốt lõi và chuyên sâu từ đoạn tài liệu trên sao cho tổng số từ của các điểm này không vượt quá ngân sách đã cho.
Mỗi TeachingPoint cần có:
- id: Một mã định danh duy nhất (ví dụ: tp_1, tp_2).
- content: Nội dung của điểm giảng dạy.

Hãy trả về kết quả dưới dạng JSON phù hợp với schema được cung cấp.
