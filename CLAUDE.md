# CLSG Script Engine

- Phạm vi 3 ngày chỉ là viết kịch bản. Không TTS, không render video, không database.
- Luôn dùng skill clsg-script-engine.
- Lệnh kiểm tra: `uv run pytest && uv run ruff check`.
